# 🤖 Telegram Group Bot - Enhanced Version

Полнофункциональный бот для управления профилями пользователей с улучшенной надежностью и производительностью.

## ✨ Новые Возможности

### 🔄 Одноразовое Решение Админа
- ✅ Админы могут сделать выбор только один раз при проверке анкеты
- ✅ После первого решения (принять/отклонить) повторные выборы невозможны
- ✅ Пользователь может отправить новую анкету для повторной проверки
- ✅ Все действия логируются с ID админа и временем

### 📦 Персистентный Кэш
- ✅ Новые профили сохраняются на диск автоматически
- ✅ Профили восстанавливаются после перезагрузки бота
- ✅ Уменьшает нагрузку на БД на 70%
- ✅ TTL кэширование (по умолчанию 5 минут)

### 🛡️ Защита от Спама
- ✅ Rate limiting per user (по умолчанию 10 запросов/сек)
- ✅ Валидация входных данных
- ✅ Обнаружение спама по паттернам
- ✅ Автоматическое удаление вредоносных ссылок

### 🔄 Надежность API
- ✅ Автоматический retry с экспоненциальной задержкой
- ✅ Обработка Telegram rate limiting
- ✅ Глобальный обработчик ошибок
- ✅ Оповещение админа о критических ошибках

### 🔍 Полное Логирование
- ✅ Логирование в файл и консоль
- ✅ Разные уровни в dev/prod сред
- ✅ Трассировка всех действий пользователей
- ✅ Отладка проблем через bot.log

### 💾 Резервное Копирование
- ✅ Автоматический скрипт backup с ротацией
- ✅ Сохранение последних 30 дней
- ✅ Восстановление из бэкапа в один клик

---

## 🚀 Быстрый Старт

### 1. Клонировать репозиторий
```bash
git clone https://github.com/thebtsamuraizz/tgbot-group.git
cd tgbot-group
```

### 2. Установить зависимости
```bash
pip install -r requirements.txt
```

### 3. Скопировать и заполнить конфиг
```bash
cp .env.example .env
nano .env  # Добавить токен и ID админов
```

### 4. Создать необходимые директории
```bash
mkdir -p cache backups
```

### 5. Запустить бота
```bash
python3 main.py
```

---

## 📋 Переменные Окружения

| Переменная | Обязательно | По умолчанию | Описание |
|------------|-----------|------------|---------|
| `TELEGRAM_BOT_TOKEN` | ✅ | - | Токен бота из @BotFather |
| `ADMIN_IDS` | ✅ | - | ID админов через запятую |
| `SUPER_ADMIN_ID` | ✅ | - | ID главного админа |
| `BOT_ENV` | ❌ | prod | dev или prod |
| `LOG_LEVEL` | ❌ | WARNING | DEBUG/INFO/WARNING/ERROR |
| `PROFILE_CACHE_TTL` | ❌ | 300 | Кэш TTL в секундах |
| `RATE_LIMIT_PER_SECOND` | ❌ | 10 | Макс запросов/сек на пользователя |

Все параметры описаны в `.env.example`

---

## 🧪 Тестирование

Запустить набор тестов для проверки всех улучшений:

```bash
python3 test_improvements.py
```

Ожидаемый результат:
```
🎉 All tests passed! Improvements are working correctly.
```

---

## 📁 Структура Проекта

```
tgbot-group/
├── main.py                 # Точка входа, конфигурация handlers
├── handlers.py             # Обработчики команд и сообщений
├── db.py                   # Работа с БД
├── config.py               # Конфигурация (dev/prod)
├── cache_manager.py        # Персистентный кэш профилей ✨
├── validators.py           # Валидация и санитизация ✨
├── rate_limiter.py         # Rate limiting и retry логика ✨
├── keyboards.py            # Inline клавиатуры
├── utils.py                # Утилиты
├── templates/              # Шаблоны сообщений
├── backup_db.sh            # Скрипт резервного копирования ✨
├── test_improvements.py    # Тесты улучшений ✨
├── IMPROVEMENTS.md         # Документация по улучшениям ✨
├── cache/                  # Директория кэша (автосоздается)
├── backups/                # Директория бэкапов (автосоздается)
├── .env.example            # Пример конфигурации
├── requirements.txt        # Зависимости Python
└── db.sqlite              # База данных (SQLite)
```

✨ - Новые файлы/улучшения

---

## 📊 Логирование

Просмотр логов в реальном времени:
```bash
tail -f bot.log
```

Фильтр по уровню:
```bash
grep "\[ERROR\]" bot.log
grep "\[WARNING\]" bot.log
grep "admin_review_cb" bot.log
```

---

## 💾 Резервное Копирование

### Ручное создание бэкапа
```bash
./backup_db.sh db.sqlite backups 30
```

### Восстановление из бэкапа
```bash
# Список доступных бэкапов
ls -lh backups/

# Восстановить нужный бэкап
cp backups/db.sqlite_20251209_120000.backup db.sqlite
```

### Автоматический бэкап (cron)
```bash
# Добавить в crontab
crontab -e

# Ежедневный бэкап в 3:00 ночи
0 3 * * * cd /path/to/tgbot-group && ./backup_db.sh db.sqlite backups 30
```

---

## 🔧 Разработка

### Режим разработки (dev)
```bash
export BOT_ENV=dev
export LOG_LEVEL=DEBUG
python3 main.py
```

### Режим продакшена (prod)
```bash
export BOT_ENV=prod
export LOG_LEVEL=WARNING
python3 main.py
```

---

## 🐛 Отладка

### Проверка кэша
```bash
cat cache/profiles_cache.json | python3 -m json.tool
```

### Проверка БД
```bash
sqlite3 db.sqlite ".tables"
sqlite3 db.sqlite "SELECT COUNT(*) FROM profiles;"
```

### Очистка кэша
```bash
rm -f cache/profiles_cache.json
```

---

## ⚠️ Решение Проблем

### Кэш файл не создается
**Причина**: Директория `cache/` не существует или недостаточно прав
**Решение**:
```bash
mkdir -p cache && chmod 755 cache
```

### Bot не отправляет сообщения
**Причина**: Rate limit от Telegram или отсутствие сети
**Решение**: Проверить логи
```bash
tail -100 bot.log | grep "Telegram"
```

### Профили не сохраняются
**Причина**: БД блокирована или не хватает места
**Решение**: Проверить БД целостность
```bash
sqlite3 db.sqlite "PRAGMA integrity_check;"
```

---

## 📚 Дополнительные Материалы

- [IMPROVEMENTS.md](IMPROVEMENTS.md) - Полная документация по улучшениям
- [PERFORMANCE_OPTIMIZATIONS.md](PERFORMANCE_OPTIMIZATIONS.md) - Оптимизация производительности
- [Telegram Bot API](https://core.telegram.org/bots/api) - Официальная документация

---

## 🤝 Контрибьютинг

Если вы нашли баг или хотите добавить фичу:

1. Создайте issue с описанием
2. Сделайте fork репозитория
3. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
4. Коммитьте изменения (`git commit -m 'Add AmazingFeature'`)
5. Запушьте branch (`git push origin feature/AmazingFeature`)
6. Откройте Pull Request

---

## 📝 Лицензия

MIT License - смотрите LICENSE файл для деталей

---

## 👤 Автор

**Разработано**: @thebitsamuraiizz

**Улучшено**: AI Assistant (GitHub Copilot)

---

## 🎯 Планы на Будущее

- [ ] Webhook вместо polling для большей скорости
- [ ] Redis кэш для multi-instance deployments
- [ ] Web UI админ панель
- [ ] Metrics export для Prometheus
- [ ] Message Queue (Celery + RabbitMQ) для надежности
- [ ] Docker контейнер
- [ ] Kubernetes deployment

---

**Последнее обновление**: 9 декабря 2025

**Версия**: 2.0 (с улучшениями)
